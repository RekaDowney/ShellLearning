#!/bin/bash

# 导入 base.sh 脚本
source /learning/base.sh

# 导入通用函数库
source /etc/init.d/functions

## currentScript 变量存储当前脚本的绝对路径，只读
readonly currentScript=$(currentScript)





function main() {
    return $?
}

main $*
exit $?

